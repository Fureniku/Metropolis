package com.fureniku.metropolis.enums;

/**
 * Enum to specify which direction a block should offset into another block
 */
public enum BlockOffsetDirection {
    NONE,
    DOWN,
    BACK,
    LEFT,
    FORWARD,
    RIGHT,
    UP
}
